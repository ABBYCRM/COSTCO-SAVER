# COSTCO-SAVER — 21st.dev component register

Spec §63. Every 21st.dev component used in the app is recorded here.

| Feature | Component | 21st.dev source | Author | License / usage status | Date obtained | Local path | Modifications |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TBD | TBD | TBD | TBD | TBD | TBD | TBD | TBD |

Until the 21st.dev marketplace plugin is configured in CI, the app ships
with the built-in Ionic React component library (which is MIT-licensed
and ships with the `@ionic/react` package). When a 21st.dev component
is added, populate the row above and link the install command in the
PR description (e.g. `npx shadcn@latest add <component>` for the
relevant registry, or the equivalent 21st.dev command).
